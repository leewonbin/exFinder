<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.exfinder.dao.BoardDao">

	<!-- 게시글 생성 -->
	<insert id="create">
		INSERT INTO board(b_id, b_title, b_content,
		b_category, b_view, u_id, b_del)
		VALUES(board_num.NEXTVAL, #{b_title},
		#{b_content}, #{b_category}, #{b_view}, #{u_id}, #{b_del})
	</insert>


	<!-- 게시글 조회 -->
	<select id="read" resultType="com.exfinder.dto.BoardDto">
		select * from board where b_id =
		#{b_id}
	</select>


	<!-- 게시글 수정 -->
	<update id="update">
		update board set b_category=#{b_category},b_title = #{b_title},
		b_content
		=#{b_content} where b_id = #{b_id}
	</update>


	<!-- 게시글 삭제 -->
	<delete id="delete">
		delete from board where b_id = #{b_id}
	</delete>


	<!-- 게시글 전체 조회 -->
	<select id="listAll" resultType="com.exfinder.dto.BoardDto">
		<![CDATA[ select * from board where b_id > 0 order by b_id]]>
	</select>

	<!-- 카테고리별 게시글 수 조회 -->
	<select id="categoryCnt" resultType="Integer">
		<![CDATA[select count(*) from board where 1=1]]>
		<if test="bCategory != null and bCategory != ''">
			AND b_category = #{bCategory}
		</if>
	</select>


	<!-- 게시글 조회수 -->
	<update id="b_viewUpdate">
		update board set b_view=b_view+1 where b_id=#{b_id}
	</update>


	<!-- 게시글 검색 -->
	<select id="listSearch" resultType="com.exfinder.dto.BoardDto"> 
		<![CDATA[SELECT * FROM board WHERE 1=1]]>

		<if test="bCategory != null and bCategory != ''">
			AND b_category = #{bCategory}
		</if>

		<if test="keyword != null and keyword != ''">
			AND b_title LIKE '%' || #{keyword} || '%'
		</if>
		
    	<![CDATA[order by b_id asc OFFSET (#{page} - 1) * #{perPageNum} ROWS FETCH NEXT #{perPageNum} ROWS ONLY]]>
	</select>


	<sql id='category'>
		<if test="categoryType!=null"> and b_category = #{b_category}</if>
	</sql>

	<sql id="search">
		<if test="searchKeyword != null"> AND bTitle LIKE '%' || #{searchKeyword} || '%' </if>
	</sql>

</mapper>
